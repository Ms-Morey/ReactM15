import React, { createContext } from 'react'
export const context = createContext();

const Task10ThemeContext = () => {
  return (
    <div>
        <context.Provider>
            
        </context.Provider>
    </div>
  )
}

export default Task10ThemeContext